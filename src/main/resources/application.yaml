spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/citlist-db
    driver-class-name: org.postgresql.Driver
    username: admin
    password: root
  sql:
    init:
      mode: always
  flyway:
    schemas:
      - citlist
      - public